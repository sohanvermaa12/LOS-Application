import { ArrowUpRight, BarChart3 } from 'lucide-react';
import AppShell from '../../components/AppShell';
import PageHeader from '../../components/PageHeader';
import Card from '../../components/Card';
import { api } from '../../services/api.js';

export default function ReportsPage() {
  return <AppShell title="Reports"><PageHeader eyebrow="INSIGHTS" title="Reports" description="Track credit performance, turnaround and portfolio health." action={<button className="secondary-button"><ArrowUpRight size={16} /> Export report</button>} /><div className="report-tabs"><button className="report-tab active">Portfolio overview</button><button className="report-tab">TAT analysis</button><button className="report-tab">Branch performance</button></div><section className="report-grid"><Card className="chart-panel"><div className="panel-heading"><div><h2>Applications over time</h2><p>Monthly applications received and approved</p></div><BarChart3 size={18} className="muted-icon" /></div><div className="chart"><div className="chart-y"><span>120</span><span>80</span><span>40</span><span>0</span></div><div className="chart-body"><div className="grid-lines"><i /><i /><i /><i /></div><div className="bars">{[['Jan', 54, 38], ['Feb', 68, 44], ['Mar', 62, 49], ['Apr', 82, 61], ['May', 76, 66], ['Jun', 96, 72]].map(([month, received, approved]) => <div className="bar-group" key={month}><div className="bar-pair"><i style={{ height: `${received}%` }} /><i style={{ height: `${approved}%` }} /></div><span>{month}</span></div>)}</div></div></div></Card><Card className="score-panel"><div className="panel-heading"><div><h2>Portfolio health</h2><p>Current quality indicators</p></div></div><div className="health-score"><div className="score-ring"><strong>82</strong><span>/ 100</span></div><div><strong className="health-good">Healthy</strong><p>+4.6 pts from last month</p></div></div><div className="health-list"><span>Approval rate <strong>38.7%</strong></span><span>First-time-right <strong>71.4%</strong></span><span>Portfolio at risk <strong>2.1%</strong></span></div></Card></section><Card className="table-panel"><div className="panel-heading"><div><h2>Branch performance</h2><p>Comparison for current month</p></div></div><div className="table-scroll"><table><thead><tr><th>Branch</th><th>Applications</th><th>Approved</th><th>Approval rate</th><th>Avg. TAT</th></tr></thead><tbody>{api.branches.map((branch) => <tr key={branch.name}><td><strong>{branch.name}</strong><span className="sub-text">{branch.code}</span></td><td>{branch.applications}</td><td>{branch.approved}</td><td><strong>{branch.rate}</strong></td><td>{branch.tat}</td></tr>)}</tbody></table></div></Card></AppShell>;
}